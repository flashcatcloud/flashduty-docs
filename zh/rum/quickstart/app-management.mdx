---
title: "应用管理"
description: "学习如何在 Flashduty 平台创建和管理 RUM 应用，包括应用创建、配置和权限管理"
keywords: ["RUM", "快速开始", "用户监控", "前端监控"]
---

## 概述

RUM 应用是承载前端性能监控数据的容器，用于采集、存储和分析用户在前端应用中的真实体验数据。一个应用代表一个被监控的前端项目，可以是网站、移动应用或单页应用等。

<Tip>
我们建议按照业务系统或应用来创建 RUM 应用，例如：官网、商城、管理后台等。
</Tip>

每个应用拥有独立的 `applicationId` 和 `clientToken`，用于识别数据来源并确保数据安全。应用创建后，您需要将 SDK 集成到您的前端代码中，以开始数据采集和监控。

## 应用权限

为了满足不同业务场景的数据安全需求，RUM 应用提供了灵活的访问级别设置：

| 访问级别 | 可见范围 | 适用场景 |
|----------|----------|----------|
| **公开** | 账户内所有用户可见，可查看数据和处理 Issue | 通用业务应用 |
| **私有** | 仅创建者、账户管理员和主体账户可见 | 敏感业务数据 |

<Note>
私有应用中，其他成员若需查看内容，可以通过分享故障链接的方式临时授权访问。
</Note>

## 创建应用

<Frame caption="RUM 应用创建界面">
<img src="https://docs-cdn.flashcat.cloud/images/png/69baa5066dae4641adf1f769f3aacc54.png" alt="RUM 应用创建界面" />
</Frame>

通过 RUM 产品引导页面，您可以快速创建一个应用：

<Steps>
<Step title="选择应用类型">
选择应用对应的前端技术类型，目前支持 **JavaScript (JS)、Android、iOS**。
</Step>

<Step title="设置管理团队">
指定该应用的管理团队。

<Warning>
团队所属成员对该应用拥有全部操作权限，非团队成员对该应用的配置仅可只读访问。
</Warning>
</Step>

<Step title="配置地理信息">
默认情况下，自动启用用户地理位置数据采集。如需禁用客户端 IP 或地理位置数据的自动采集，请关闭地理信息收集开关。

详见 [数据收集](/zh/rum/others/data-collection)。
</Step>

<Step title="配置告警">
默认情况下，自动开启告警通知，方便您及时处理错误。

详见 [Issue 告警](/zh/rum/error-tracking/issue-alerts)。
</Step>
</Steps>

{/* REVIEW: Auto-generated stub from coverage-audit. This feature exists in code but lacks documentation. */}
## 编辑应用信息

{/* REVIEW: 功能概述 — 应用创建后，您可以随时修改应用的基本信息 */}

您可以在「应用管理」中编辑已有应用的基本信息：

| 字段 | 说明 |
|------|------|
| **应用名称** | {/* REVIEW: 修改应用的显示名称 */} |
| **管理团队** | {/* REVIEW: 重新指定应用所属的管理团队 */} |
| **访问级别** | {/* REVIEW: 切换公开/私有访问级别 */} |
{/* END REVIEW */}

{/* REVIEW: Auto-generated stub from coverage-audit. This feature exists in code but lacks documentation. */}
## Tracing 设置

{/* REVIEW: 功能概述 — 在应用设置中配置 Tracing 跳转功能，用于关联到外部监控系统的链路追踪页面 */}

在「应用管理」-「Tracing 设置」中，您可以配置分布式追踪的跳转行为：

| 字段 | 说明 |
|------|------|
| **启用开关** | {/* REVIEW: 开启或关闭 Tracing 跳转功能 */} |
| **Endpoint URL** | {/* REVIEW: 配置 Tracing 跳转的目标地址 */} |
| **打开方式** | {/* REVIEW: 选择跳转方式：弹窗（Popup）或新标签页（Tab） */} |

<Tip>
SDK 侧的 Tracing 配置（如 `allowedTracingUrls`、`traceSampleRate`）请参见[分布式追踪最佳实践](/zh/rum/best-practices/distributed-tracing)。
</Tip>
{/* END REVIEW */}

{/* REVIEW: Auto-generated stub from coverage-audit. This feature exists in code but lacks documentation. */}
## 隐私设置

{/* REVIEW: 功能概述 — 在应用设置中管理用户隐私数据的采集行为 */}

在「应用管理」-「隐私设置」中，您可以控制以下用户数据的采集：

| 开关 | 控制字段 | 说明 |
|------|----------|------|
| **地理位置信息采集** | `@geo_country`、`@geo_province`、`@geo_city` | {/* REVIEW: 开启后采集用户的国家、省份、城市信息 */} |
| **IP 地址采集** | `@geo_ip_v4`、`@geo_ip_v6` | {/* REVIEW: 开启后采集用户的 IPv4 和 IPv6 地址 */} |

<Note>
这些设置可在应用创建后随时修改，修改后立即生效。
</Note>
{/* END REVIEW */}

## SDK 配置

<video
  controls
  className="w-full aspect-video rounded-xl"
  src="https://docs-cdn.flashcat.cloud/videos/app-sdk-config.mov"
></video>

您可以在 **应用配置 > SDK 配置** 中修改参数并实时预览初始化代码，以便快速接入 SDK。

<Warning>
在应用管理中修改 SDK 配置并不会实时生效到已集成的客户端。所有配置更改需要在您的前端代码中更新并重新部署才能生效。
</Warning>

### 服务定义

服务是一个独立的、可部署的代码存储库，它映射到一组页面。

<Tabs>
<Tab title="单体应用">
如果您的应用程序是作为一个整体构建的，那么您的 RUM 应用只需要一个服务名称。

```javascript
flashcatRum.init({
  applicationId: 'YOUR_APP_ID',
  clientToken: 'YOUR_CLIENT_TOKEN',
  service: 'my-web-app'  // 单一服务名称
});
```
</Tab>

<Tab title="微前端/多页面应用">
如果您的浏览器应用程序由多个独立存储库构建，请为不同模块设置不同的服务名称。

```javascript
// 主应用
flashcatRum.init({
  service: 'main-app'
});

// 子应用 A
flashcatRum.init({
  service: 'module-a'
});
```
</Tab>
</Tabs>

## 下一步

<CardGroup cols={3}>
<Card title="SDK 接入指南" icon="code" href="/zh/rum/sdk/web/sdk-integration">
  了解如何接入 RUM SDK
</Card>

<Card title="高级配置" icon="sliders" href="/zh/rum/sdk/web/advanced-config">
  了解 SDK 的高级配置选项
</Card>

<Card title="分析看板" icon="chart-line" href="/zh/rum/analytics/web">
  查看和分析 RUM 数据
</Card>
</CardGroup>
